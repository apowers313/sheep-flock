var program = require('commander');

program
    .version('0.0.1')
    .description("A distributed, high-definition renderer for Electric Sheep");

// API token
// Private key (default: ~/.ssh/id_rsa)
// Private key fingerprint
// Concurrency (default: 10)
// Proxy (default: true; --no-proxy)